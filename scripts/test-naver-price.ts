// @ts-nocheck
export { };
import { getMarketAnalysis, fetchNaverPrice } from '../app/lib/naver';
import 'dotenv/config';

async function testNaverPrice() {
    console.log("Testing Naver Price Fetch...");
}
// I'm potentially destroying the script content here but they are just test scripts.
// The user doesn't use them in production.
